module TSCompiler {
	export class Lexer {

		// Separates the input code into a list of tokens and returns that list
		// TODO: Want to return a list of Token types
		public static tokenizeCode(inputCode: string): string [] {

			console.log("In tokenizeCode()");
			Logger.write("Beginning to perform lexical analysis.");

			return [];
		}
	}
}
